# runtime state — cleanable between attempts (`rm -rf state sandbox`).
# Heavy artifact blobs stay out of git; a representative state/memory.json + the
# sandbox reminder files from the Query-C run are committed as durable-memory
# evidence (assignment deliverable: "state/ visible").
state/artifacts/
usage.json

# secrets
.env

# python
__pycache__/
*.pyc
.venv/
.uv/

# PROVIDED course code — dropped in locally, NOT redistributed here.
# Get these from the Session 6 lesson; place mcp_server.py next to this file and
# run the gateway from its own folder.
mcp_server.py
llm_gatewayV3/
llm_gatewayV2/

# runtime state — excluded by .gitignore per the assignment submission rubric
# ("the state/ directory excluded by .gitignore"). Cleanable between attempts
# with `rm -rf state sandbox`.
state/
sandbox/
usage.json

# secrets
.env

# python
__pycache__/
*.pyc
.venv/
.uv/

# PROVIDED course code — dropped in locally, NOT redistributed here.
# Get these from the Session 6 lesson; place mcp_server.py next to this file and
# run the gateway from its own folder.
mcp_server.py
llm_gatewayV3/
llm_gatewayV2/
